#!/bin/bash
export DISPLAY=:0 ###need to export display so cron has path for x
#HOUR=$[ $(date +"%k") % 2 ]
#if [ $HOUR -ne "0" ]; then
conky -c /etc/conky/pacupdis_conky.conf
sleep 300
pkill -f 'conky -c /etc/conky/pacupdis_conky.conf'

